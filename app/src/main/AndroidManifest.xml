<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onegmall">
    
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Checkout2"></activity>
        <activity android:name=".Checkout" />
        <activity android:name=".TopUpLifeafter" />
        <activity android:name=".TopUpDonut" />
        <activity android:name=".LifeafterCurrencyCheck" />
        <activity android:name=".DonutCurrencyCheck" />
        <activity android:name=".LogOut" />
        <activity android:name=".Feedback" />
        <activity android:name=".History" />
        <activity android:name=".Menu" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>